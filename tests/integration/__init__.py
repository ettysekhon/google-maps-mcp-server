"""
Integration tests for Google Maps MCP Server.

Integration tests make real API calls to Google Maps Platform
and require a valid GOOGLE_MAPS_API_KEY to be set.

These tests are marked with @pytest.mark.integration and are
skipped by default in CI to avoid API charges.
"""
